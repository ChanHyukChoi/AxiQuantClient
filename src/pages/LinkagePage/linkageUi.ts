import type { CSSProperties } from 'react'

/** CardsPage·Grid와 동일 — 페이지 본문 15px, 그리드 헤더 16px */
export const LINKAGE_FONT_SIZE = 15
export const LINKAGE_GRID_HEADER_FONT_SIZE = 16

export const linkagePanelHeaderStyle: CSSProperties = {
  background: 'var(--color-accent-subtle)',
  color: 'var(--color-accent)',
  borderBottom: '0.5px solid var(--color-border)',
}
